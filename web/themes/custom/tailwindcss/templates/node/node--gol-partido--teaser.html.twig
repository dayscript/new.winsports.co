<div class="gol-partido teaser">
    <div class="flex items-top">
        <div class="image w-1/2">
            {% if node.hasField('field_image_h') %}
                {{ content.field_image_h }}
            {% elseif node.hasField('field_image') %}
                {{ content.field_image }}
            {% endif %}
        </div>
        <div class="w-1/2 text-sm pl-2">
            <a href="{{ url }}">{{ label }}</a>
        </div>
    </div>
</div>