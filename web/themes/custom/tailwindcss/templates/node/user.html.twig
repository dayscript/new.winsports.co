<div class="user compact" {{ attributes }}>
    <div class="tw-flex flex text-sm">
        <div class="image self-center tw-w-2/12">{{ content.field_image }}</div>
        <div class="text-sm tw-w-10/12">
            <div class="nombres self-center lead">{{ content.field_nombres }}</div>
            <div class="twitter self-center">{{ content.field_twitter }}</div>   
        </div>
    </div>
</div>