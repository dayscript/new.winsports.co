<div class="galeria teaser" {{ attributes.addClass(classes) }}>
    <div class="image">
        {% if node.hasField('field_image') %}
            {{ content.field_image }}
        {% endif %}
    </div>
    <div class="tw-text-sm">
        <a href="{{ url }}">{{ label }}</a>
    </div>
</div>